package feb_week3_13_feb_2023.Assignment10;


/* Var Args ==>
        In Java, a varargs (variable-length arguments)
        parameter allows a method to take a variable number
         of arguments of the same type.
         A method with a varargs parameter is declared using an ellipsis (...) after the parameter type.
        For example:
        public void foo(String... args) {
                // method body
            }

       Covarient Return Type:
         The covariant return type specifies that the return type may vary in the same direction as the subclass.
                Before Java5, it was not possible to override any method by
                changing the return type.
                 But now, since Java5, it is possible to override method by changing the return type
                if subclass overrides any method whose return type is Non-Primitive but it changes its return type to subclass type.


* */
public class VarargsCovarient {
    public static void main(String[] args) {

    }
}
